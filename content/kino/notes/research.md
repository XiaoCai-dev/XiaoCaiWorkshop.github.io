# Research

Things to read / try.

## Multi-agent debate

Do two planner variants argue over the next step and pick a winner? Probably
too slow for realtime, maybe useful for hard cuts.

## Graph compression

Long runs produce large graphs. Can we summarize completed subgraphs into a
single "macro-task" without losing replay fidelity?

## Streaming assembly

Instead of assemble-at-end, assemble incrementally as clips land. Lets the user
scrub a partial timeline while generation continues.
